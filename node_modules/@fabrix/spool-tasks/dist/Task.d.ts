import { FabrixApp } from '@fabrix/fabrix';
import { FabrixGeneric } from '@fabrix/fabrix/dist/common';
export declare class Task extends FabrixGeneric {
    message: any;
    isAcknowledged: any;
    private _id;
    constructor(app: FabrixApp, message: any);
    id: any;
    readonly name: string;
    ack(): Promise<any>;
    nack(): Promise<any>;
    reject(): Promise<any>;
    run(): Promise<void>;
    interrupt(msg: any): Promise<void>;
    finalize(results: any): Promise<void>;
}
