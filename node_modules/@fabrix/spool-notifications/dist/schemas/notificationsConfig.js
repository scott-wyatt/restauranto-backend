"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const joi = require("joi");
exports.notificationsConfig = joi.object().keys({
    prefix: joi.string().allow('', null).required(),
    to: joi.object().keys({
        default_name: joi.string()
    }),
    from: joi.object().keys({
        name: joi.string(),
        email: joi.string()
    })
});
