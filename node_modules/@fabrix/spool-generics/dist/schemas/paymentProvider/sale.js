"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const joi = require("joi");
exports.sale = joi.object().keys({
    amount: joi.number().required(),
    payment_details: joi.object().keys({
        gateway_token: joi.any(),
        source: joi.any(),
        gateway: joi.string()
    }).required()
}).unknown();
