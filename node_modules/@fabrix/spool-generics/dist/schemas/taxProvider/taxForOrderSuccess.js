"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const joi = require("joi");
exports.taxForOrderSuccess = joi.object().keys({
    total_taxes: joi.number(),
    tax_lines: joi.array().items(joi.object().keys({
        name: joi.string(),
        price: joi.number()
    }).unknown()),
    line_items: joi.array().items(joi.object().keys({
        total_taxes: joi.number(),
        tax_lines: joi.array().items(joi.object().keys({
            name: joi.string(),
            price: joi.number()
        }))
    }).unknown())
}).unknown();
