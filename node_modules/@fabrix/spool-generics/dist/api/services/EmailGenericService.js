"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const common_1 = require("@fabrix/fabrix/dist/common");
const validator_1 = require("../../validator");
class EmailGenericService extends common_1.FabrixService {
    _init(generic) {
        const Adapter = generic ? generic.adapter : this.app.config.get('generics.email_provider.adapter');
        return new Adapter(this.app, generic ? generic.config : this.app.config.get('generics.email_provider.config'));
    }
    send(data, adapter) {
        return validator_1.Validator.validateEmailProvider.send(data)
            .then(values => {
            adapter = this._init(adapter);
            return adapter.send(data)
                .then(response => {
                return validator_1.Validator.validateEmailProvider.sendSuccess(response);
            });
        });
    }
    sendTemplate(data, adapter) {
        return validator_1.Validator.validateEmailProvider.sendTemplate(data)
            .then(values => {
            adapter = this._init(adapter);
            return adapter.sendTemplate(data)
                .then(response => {
                return validator_1.Validator.validateEmailProvider.sendTemplateSuccess(response);
            });
        });
    }
}
exports.EmailGenericService = EmailGenericService;
