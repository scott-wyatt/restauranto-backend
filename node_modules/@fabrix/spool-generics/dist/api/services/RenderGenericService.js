"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const common_1 = require("@fabrix/fabrix/dist/common");
const validator_1 = require("../../validator");
class RenderGenericService extends common_1.FabrixService {
    _init(generic) {
        const Adapter = generic ? generic.adapter : this.app.config.get('generics.render_service.adapter');
        return new Adapter(this.app, generic ? generic.config : this.app.config.get('generics.render_service.config'));
    }
    render(data, adapter) {
        return validator_1.Validator.validateRenderService.render(data)
            .then(values => {
            adapter = this._init(adapter);
            return adapter.render(data)
                .then(docObj => {
                return validator_1.Validator.validateRenderService.renderSuccess(docObj);
            });
        });
    }
    renderSync(data, adapter) {
        adapter = this._init(adapter);
        return adapter.renderSync(data);
    }
}
exports.RenderGenericService = RenderGenericService;
