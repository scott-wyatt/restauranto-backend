import { Generic } from '../Generic';
export declare class TaxDefault extends Generic {
    getRate(data: any): Promise<{
        amount: number;
        rate: number;
        title: string;
        tax_details: {};
    }>;
    taxForOrder(data: any): Promise<{
        total_taxes: number;
        tax_lines: {
            name: string;
            price: number;
        }[];
        line_items: any;
    }>;
}
