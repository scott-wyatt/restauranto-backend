import { Generic } from '../Generic';
export declare class DataStoreDefault extends Generic {
    upload(buffer: any): Promise<{
        status: string;
        url: string;
    }>;
    uploadFile(file: any): Promise<{
        status: string;
        url: any;
    }>;
    uploadFiles(files: any): Promise<any>;
}
