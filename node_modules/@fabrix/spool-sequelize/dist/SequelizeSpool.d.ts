import { DatastoreSpool } from '@fabrix/fabrix/dist/common/spools/datastore';
import * as Sequelize from 'sequelize';
export declare class SequelizeSpool extends DatastoreSpool {
    _datastore: Sequelize.SequelizeStatic;
    private _connections;
    private _models;
    constructor(app: any);
    readonly models: {
        [key: string]: any;
    };
    readonly connections: {
        [key: string]: any;
    };
    validate(): Promise<{}[]>;
    configure(): void;
    initialize(): Promise<any[]>;
    unload(): Promise<any[]>;
    migrate(): Promise<any[]>;
}
