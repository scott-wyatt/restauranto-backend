import { ServerSpool } from '@fabrix/fabrix/dist/common/spools/server';
import { Express } from 'express';
export declare class ExpressSpool extends ServerSpool {
    server: Express;
    constructor(app: any);
    validate(): Promise<{}[]>;
    configure(): void;
    initialize(): Promise<void>;
    unload(): Promise<void>;
    sanity(): void;
}
