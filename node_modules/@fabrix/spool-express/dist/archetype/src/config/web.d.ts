import * as express from 'express';
export declare const web: {
    express: typeof express;
    cors: boolean;
    middlewares: {};
    cache: number;
    port: string | number;
    host: string;
};
