/// <reference types="node" />
import { FabrixApp } from '@fabrix/fabrix';
import { Express, Request, Response } from 'express';
import * as http from 'http';
import * as express from 'express';
export declare const web: {
    init: (app: FabrixApp, expressApp: Express) => boolean;
    middlewares: {
        compression: express.RequestHandler;
        methodOverride: express.RequestHandler;
        cookieParser: express.RequestHandler;
        '404': (req: Request, res: Response, next: any) => void;
        '500': (error: any, req: Request, res: Response, next: any) => void;
        order: string[];
        bodyParser: ((req: http.IncomingMessage, res: http.ServerResponse, next: (err?: any) => void) => void)[];
    };
    cache: number;
    host: string;
    port: string | number;
};
