import { ExtensionSpool } from '@fabrix/fabrix/dist/common/spools/extension';
export declare class CachesSpool extends ExtensionSpool {
    cacheInstances: {
        [key: string]: any;
    };
    constructor(app: any);
    validate(): Promise<{}[]>;
    configure(): void;
    initialize(): Promise<void>;
    unload(): Promise<any[]>;
}
