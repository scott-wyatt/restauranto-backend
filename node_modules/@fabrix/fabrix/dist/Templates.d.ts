import { FabrixApp } from './index';
export declare const Templates: {
    hr: string;
    docs: string;
    info: {
        start: string;
        stop: string;
        initialized: string;
        sane: string;
        ready(app: FabrixApp): string;
    };
    debug: {
        ready(app: FabrixApp): string;
    };
    silly: {
        ready(app: FabrixApp): string;
        sane: string;
        initialized: string;
    };
};
