import { Spool } from '../../../';
export declare class Archetype extends Spool {
    static readonly lifecycle: {};
    constructor(app: any);
    validate(): void;
    configure(): void;
    initialize(): Promise<void>;
    unload(): Promise<void>;
}
